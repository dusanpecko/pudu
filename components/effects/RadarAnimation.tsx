/**
 * The glowing sphere with two counter-rotating dashed rings behind the robot.
 * Purely decorative; the rings are drawn by CSS pseudo-elements.
 */
export default function RadarAnimation() {
  return <div className="orb" aria-hidden="true" />;
}
