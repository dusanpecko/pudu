# Absolute origin used for canonical URLs, hreflang alternates, Open Graph
# images, sitemap.xml and robots.txt. No trailing slash.
# Falls back to http://localhost:3000 when unset.
NEXT_PUBLIC_SITE_URL=https://www.example.com
